import { OngsPrismaRepository } from './ongs-prisma-repository';

describe('OngsPrismaRepository', () => {
  it('should be defined', () => {
    expect(new OngsPrismaRepository()).toBeDefined();
  });
});
