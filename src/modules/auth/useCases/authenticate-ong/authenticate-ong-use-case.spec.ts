import { AuthenticateOngUseCase } from './authenticate-ong-use-case';

describe('AuthenticateOngUseCase', () => {
  it('should be defined', () => {
    expect(new AuthenticateOngUseCase()).toBeDefined();
  });
});
