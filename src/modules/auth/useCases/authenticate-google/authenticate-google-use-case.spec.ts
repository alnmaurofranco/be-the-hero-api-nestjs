import { AuthenticateGoogleUseCase } from './authenticate-google-use-case';

describe('AuthenticateGoogleUseCase', () => {
  it('should be defined', () => {
    expect(new AuthenticateGoogleUseCase()).toBeDefined();
  });
});
