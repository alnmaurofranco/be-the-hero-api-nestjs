import { UpdateIncidentUseCase } from './update-incident-use-case';

describe('UpdateIncidentUseCase', () => {
  it('should be defined', () => {
    expect(new UpdateIncidentUseCase()).toBeDefined();
  });
});
