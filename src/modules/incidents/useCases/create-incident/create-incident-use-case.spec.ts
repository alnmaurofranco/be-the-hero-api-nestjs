import { CreateIncidentUseCase } from './create-incident-use-case';

describe('CreateIncidentUseCase', () => {
  it('should be defined', () => {
    expect(new CreateIncidentUseCase()).toBeDefined();
  });
});
