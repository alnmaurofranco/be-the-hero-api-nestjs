import { GetAllIncidentUseCase } from './get-all-incident-use-case';

describe('GetAllIncidentUseCase', () => {
  it('should be defined', () => {
    expect(new GetAllIncidentUseCase()).toBeDefined();
  });
});
